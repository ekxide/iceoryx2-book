# The ROS 2 snippets build against the Rust message crates of a sourced ROS 2
# environment, so they form a workspace of their own, outside the snippets
# workspace that builds without ROS 2.
[workspace]
resolver = "2"
members = [
    "gateway_basics",
    "shouter",
    "twist_limiter",
]
